import { ReadkeypipePipe } from './readkeypipe.pipe';

describe('ReadkeypipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReadkeypipePipe();
    expect(pipe).toBeTruthy();
  });
});
