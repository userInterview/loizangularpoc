import { DirurltitreDirective } from './dirurltitre.directive';

describe('DirurltitreDirective', () => {
  it('should create an instance', () => {
    const directive = new DirurltitreDirective();
    expect(directive).toBeTruthy();
  });
});
