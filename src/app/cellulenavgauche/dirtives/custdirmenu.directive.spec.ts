import { CustdirmenuDirective } from './custdirmenu.directive';

describe('CustdirmenuDirective', () => {
  it('should create an instance', () => {
    const directive = new CustdirmenuDirective();
    expect(directive).toBeTruthy();
  });
});
